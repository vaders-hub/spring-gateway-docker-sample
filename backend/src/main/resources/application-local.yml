# 6단계: local profile에서 공통 application.yml에 덮어쓰는 설정.
# 로컬 학습용 DEBUG 로그와 tracing sampling. 수집 서버/export 활성화는 별도 설정이다.
management:
  tracing:
    sampling:
      probability: 1.0

logging:
  level:
    com.example.backend: DEBUG
