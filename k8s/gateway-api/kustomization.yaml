apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# Optional resources: apply only after the base and Envoy Gateway CRDs/controller.
# Namespace is explicit on namespaced CRs; GatewayClass is cluster-scoped.
resources:
  - gateway-class.yaml
  - envoy-proxy.yaml
  - edge-gateway.yaml
  - http-route.yaml
