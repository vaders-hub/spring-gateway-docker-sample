# 3단계: base에 local namespace와 요청 제한 patch를 합쳐 배포할 YAML을 만든다.
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gateway-lab
resources:
  - ../../base
patches:
  - path: rate-limit-patch.yaml
