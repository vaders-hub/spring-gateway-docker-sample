app:
  security:
    cors:
      allowed-origins: ${CORS_ALLOWED_ORIGINS}

logging:
  level:
    root: INFO
    com.example.gateway: INFO
    org.springframework.cloud.gateway: WARN
  structured:
    format:
      console: ecs
